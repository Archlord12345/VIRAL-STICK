/**
 * Viral Stick — Web Theme System
 * Uses shared Design Tokens
 */
import { colors, spacing, borderRadius, shadows } from '../../../theme/tokens';

export const theme = {
  colors,
  spacing,
  borderRadius,
  shadows,
};

export default theme;
