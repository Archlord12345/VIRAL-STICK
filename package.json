{
  "scripts": {
    "start": "npx concurrently -n BE,FE -c cyan,magenta \"npm start --prefix backend\" \"cd web && PORT=3001 npx react-scripts start\"",
    "build": "cd web && npm install && CI=false DISABLE_ESLINT_PLUGIN=true npm run build && cd .. && rm -rf build && mv web/build build",
    "test": "jest"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "jest": "^30.4.2",
    "prettier": "^3.8.4"
  }
}
