{
  "scripts": {
    "test": "jest",
    "build": "cd web && npm run build && mv build ../build"
  },
  "devDependencies": {
    "eslint": "^10.5.0",
    "jest": "^30.4.2",
    "prettier": "^3.8.4"
  }
}
